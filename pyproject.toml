[project]
name = "brave-tab-standby"
version = "0.3.0"
description = "Python harness for a Brave/Chrome tab standby extension"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["standby_tool"]
